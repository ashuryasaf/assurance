[phases.setup]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .next/standalone/server.js"

[variables]
NIXPACKS_NODE_VERSION = "20"
NODE_ENV = "production"
PORT = "3000"
HOSTNAME = "0.0.0.0"
